package com.mesa.agenda.todo.domain;
import com.vaadin.flow.component.template.Id;
import jakarta.persistence.Entity;

import java.time.LocalDateTime;
/*
@Entity
public class Task {

    //do we need the same @Id from student? Or is it shared by Class.java? etc.
    //private long id;

    private String taskName;
    private String location;
    private boolean isComplete;
    private LocalDateTime dateCreated;
    private LocalDateTime dueDate;

    //full constructor
    //@TODO: still needs @Id and foreign key values added to constructor
    public Task(String taskName, String location, boolean isComplete, LocalDateTime dateCreated, LocalDateTime dueDate) {
        this.taskName = taskName;
        this.location = location;
        this.isComplete = false;
        this.dateCreated = dateCreated;
        this.dueDate = dueDate;
    }

    //getters and setters
    //still missing getters/setters for Id and classId FK
    public String getTaskName() { return taskName; }
    public void setTaskName(String taskName) { this.taskName = taskName; }

    public String getLocation() { return location; }
    public void setLocation(String location) { this.location = location; }

    public boolean isComplete() { return isComplete; }
    public void setComplete(boolean complete) { isComplete = complete; }

    public LocalDateTime getDateCreated() { return dateCreated; }
    public void setDateCreated(LocalDateTime dateCreated) { this.dateCreated = dateCreated; }

    public LocalDateTime getDueDate() { return dueDate; }
    public void setDueDate(LocalDateTime dueDate) { this.dueDate = dueDate;}

    //@TODO: toString needs to be updated with class variables when they're added
    @Override
    public String toString() {
        return "Task{" +
                "taskName='" + taskName + '\'' +
                ", location='" + location + '\'' +
                ", isComplete=" + isComplete +
                ", dateCreated=" + dateCreated +
                ", dueDate=" + dueDate +
                '}';
    }
}
*/
